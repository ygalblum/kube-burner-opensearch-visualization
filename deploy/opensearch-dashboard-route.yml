apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: dashboard
spec:
  to:
    kind: Service
    name: burner-dashboard-opensearch-dashboards
    weight: 100
  wildcardPolicy: None
  tls:
    termination: edge
  port:
    targetPort: 5601
